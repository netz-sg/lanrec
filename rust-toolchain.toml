[toolchain]
channel = "stable"
# No `targets` pin on purpose: the receiver is built for macOS and Linux too, and
# pinning a Windows target here would make every non-Windows build install a
# cross-std it never uses.
